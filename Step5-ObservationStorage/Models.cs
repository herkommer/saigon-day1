using Microsoft.ML.Data;

// Copy SignalData and AlertPrediction from Step2

// TODO: Add Observation class
// Properties: Id (Guid), Timestamp (DateTime), Value (float), 
//             PredictedAlert (bool), Probability (float), ActualAlert (bool?)

// TODO: Add ObservationStore class
// Methods: Add(Observation), GetById(Guid), GetAll(), GetLabeled()
